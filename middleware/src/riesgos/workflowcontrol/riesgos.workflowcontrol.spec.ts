
describe('workflowcontrol test-suit', () => {

    it('write some tests', () => {
        expect(true).toBeTruthy();
    });

    // it('workflow should work with a simple graph', () => {});

    // it('should throw errors on invalid input', () => {});

    // it('should handle errors on async processes', () => {});
});